<?xml version="1.0" encoding="utf-8" ?>
<dpviews:PopupView 
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
	xmlns:dpconverters="clr-namespace:DigitalProduction.Maui.Converters;assembly=DigitalProduction.Maui.AppToolkit"
	xmlns:dpviews="clr-namespace:DigitalProduction.Maui.Views;assembly=DigitalProduction.Maui.AppToolkit"
	xmlns:viewmodel="clr-namespace:BibTeXManager.ViewModels"
	x:Class="BibTeXManager.Views.StringConstantView"
	x:DataType="viewmodel:StringViewModel"
	x:Name="self"
	CanBeDismissedByTappingOutsideOfPopup="False">

	<Border StyleClass="PopupFrameStyle" MinimumWidthRequest="570" MinimumHeightRequest="380">

		<Border.Resources>
			<ResourceDictionary>
				<dpconverters:FirstValidationErrorConverter x:Key="FirstValidationErrorConverter"/>
			</ResourceDictionary>
		</Border.Resources>

		<VerticalStackLayout StyleClass="VerticalLayoutStyle">
			<Label Text="{Binding Title}" StyleClass="Title" HorizontalOptions="Start"/>

			<VerticalStackLayout StyleClass="TitleVerticalStackStyle">
				<Label Text="Name" StyleClass="ControlLabel"/>
				<Entry
                    x:Name="NameEntry"
					Placeholder="Name"
					Loaded="OnLoadedSetFocus"
					Text="{Binding Name.Value}"
					HorizontalOptions="Fill">
					<Entry.Behaviors>
						<toolkit:EventToCommandBehavior
                            x:TypeArguments="system:EventArgs"
                            Command="{Binding ValidateNameCommand}"
                            BindingContext="{Binding Path=BindingContext, Source={x:Reference NameEntry}, x:DataType=Entry}"
                            EventName="TextChanged"/>
					</Entry.Behaviors>
				</Entry>
				<Label Text="{Binding Name.Errors, Converter={StaticResource FirstValidationErrorConverter}}" StyleClass="InvalidLabelStyle"/>
			</VerticalStackLayout>

			<VerticalStackLayout StyleClass="TitleVerticalStackStyle">
				<Label Text="Output Name" StyleClass="ControlLabel"/>
				<Entry
                    x:Name="OutputNameEntry"
					Placeholder="Output Name"
					Text="{Binding OutputName.Value}"
					HorizontalOptions="Fill">
					<Entry.Behaviors>
						<toolkit:EventToCommandBehavior
                            x:TypeArguments="system:EventArgs"
                            Command="{Binding ValidateOutputNameCommand}"
                            BindingContext="{Binding Path=BindingContext, Source={x:Reference OutputNameEntry}, x:DataType=Entry}"
                            EventName="TextChanged"/>
					</Entry.Behaviors>
				</Entry>
				<Label Text="{Binding OutputName.Errors, Converter={StaticResource FirstValidationErrorConverter}}" StyleClass="InvalidLabelStyle"/>
			</VerticalStackLayout>

			<VerticalStackLayout StyleClass="TitleVerticalStackStyle">
				<Label Text="Description" StyleClass="ControlLabel"/>
				<Entry
				    Placeholder="Description"
				    Text="{Binding Field.Description}"
				    HorizontalOptions="Fill"/>
			</VerticalStackLayout>

			<HorizontalStackLayout StyleClass="BottomButtonRowStyle">
				<Button 
					Text="OK"
					HorizontalOptions="End"
					StyleClass="StandardSizeButtonStyle"
					IsEnabled="{Binding IsSubmittable}"
					Clicked="OnSaveButtonClicked"/>
				<Button 
					Text="Cancel"
					StyleClass="StandardSizeButtonStyle"
					HorizontalOptions="End"
					Clicked="OnCancelButtonClicked"/>
			</HorizontalStackLayout>

		</VerticalStackLayout>
	</Border>

</dpviews:PopupView>
